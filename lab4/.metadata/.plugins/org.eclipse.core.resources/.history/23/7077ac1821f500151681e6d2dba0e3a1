#ifndef QUEUE_H
#define QUEUE_H

#define QLENGTH 20

typedef struct
{
    char* items[QLENGTH];
    int lengths[QLENGTH];
    char queuePointer;
} Queue;

void push(Queue*, char*);
int pop(Queue*, char*);
int isEmpty(Queue*);
int isFull(Queue*);

#endif /*QUEUE_H*/
