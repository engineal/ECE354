#ifndef ETHERNET_H_
#define ETHERNET_H_

#include "helper.h"
#include "ethernet_layer.h"
#include "IP_layer.h"
#include "UDP_layer.h"

void encode_message(char* data, int valueSize);
int decode_message(char* data, int dataLength, int port, char* ip_addr, char* ether_addr);

#endif /*ETHERNET_H_*/
