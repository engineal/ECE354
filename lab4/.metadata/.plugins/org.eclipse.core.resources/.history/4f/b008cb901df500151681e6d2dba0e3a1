#include "queue.h"

void push(Queue* queue, char* item) {
    if (!isFull(queue)) {
        queue->
    }
}

char* pop(Queue* queue) {
    
}

int isEmpty(Queue* queue) {
    return queue->queuePointer == 0;
}

int isFull(Queue* queue) {
    return queue->queuePointer == QLENGTH - 1;
}
